<% flash.each do |type, msg| %>
  <div class="alert alert-<%= bootstrap_class_for(type)%> alert-dismissible fade show my-2" role="alert"><%= msg%>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="close"></button>
  </div>
<% end %>